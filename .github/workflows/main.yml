on:
  push:
    branches:
      - master





jobs:
  bulid:
    runs-on: ubuntu-lastest

    steps: 
      - name: 'print content before'
        run: ls
      - uses: actions/checkout@v3
      
      - name: 'print content after'
        run: ls
