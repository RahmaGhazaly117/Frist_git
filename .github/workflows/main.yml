on:
  push:
    branches:
      - master





jobs:
  bulid:
    runs-on: ubuntu-lastest

    steps: 
      - uses: actions/checkout@v3
      - name: "login to docker hub"
        run: docker login --username ${{ secerts.DOCKER_USERNAME }} --password ${{ secerts.DOCKER_TOKEN }}
      
    
